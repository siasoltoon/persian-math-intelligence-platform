name: Temporary Autoformat

on:
  push:
    branches: ["main"]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install project
        run: python -m pip install --upgrade pip && python -m pip install -e ".[dev]"
      - name: Format
        run: |
          python -m ruff format .
          python -m ruff format --check . > ruff-format-report.txt 2>&1 || true
          python -m ruff check . --fix > ruff-report.txt 2>&1 || true
          cat ruff-format-report.txt
          cat ruff-report.txt
      - name: Upload ruff report
        uses: actions/upload-artifact@v4
        with:
          name: ruff-report
          path: ruff-report.txt

      - name: Upload format report
        uses: actions/upload-artifact@v4
        with:
          name: ruff-format-report
          path: ruff-format-report.txt

      - name: Commit formatting changes
        run: |
          if git diff --quiet; then
            echo "No formatting changes."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "style: apply ruff formatting"
          git push
